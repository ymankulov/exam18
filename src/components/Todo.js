import React, {useState, useEffect} from "react";
import "./Todo.css";
import {Modal} from "./Modal";

const Todo = () => {

    return (
        <section className="container">
            <Modal/>
        </section>
    );
};

export default Todo;
